{
  "name": "@vaulttap/contracts",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tact --config tact.config.json",
    "dev": "tact --config tact.config.json --watch",
    "typecheck": "tact --config tact.config.json --check",
    "lint": "echo \"Add linter if needed\""
  },
  "devDependencies": {
    "@tact-lang/compiler": "^1.5.0",
    "typescript": "^5.7.3"
  }
}
